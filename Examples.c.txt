#include<stdio.h>
#include <string.h>
/*  
  int main () {
int i;
for(i=0;i<10;i++){
  printf("ridvan arslan\n" );
}
   return 0;  } */

/*
  int main () {  
  int n,fact,i;
  printf("bir sayi gir");
  scanf("%d",&n); 

  fact = 1;
  for(i=1; i<=n; i++){
  fact *= i;
  }
  printf("%d",fact);

    return 0;  }  */

/*
  int main () {
  int pi=3,r,area;
  printf("r yi gir: \n");
  scanf("%d", &r);
  area = pi*r*r;
  printf("dairenin alani: %d", area );
  
   return 0;  } */

/*
  int main () { 
  int n;
  printf("bir sayi girin");
  scanf("%d", &n);
  if ( n%2==0) {
  printf("even sayi");
  } else {
  printf("odd sayi");  } 
  
  return 0;  } */

/*
  int main () { 
  float f,c;
  printf("bir sicaklik girin");
  scanf("%f", &f);
  c= (f-32)/1.8;
  printf("celcius : %f", c);

  return 0;  } */

/*  
  int main () {
  
  int a,b,c;
  printf("3 sayi gir \n");
  scanf("%d%d%d", &a,&b,&c);
  if(a>b && a>c ) 
  printf("en buyuk sayi: %d", a);
  else if ( b>a && b>c)
  printf(" en buyuk sayi: %d", b);
  else if ( c>a && c>b)
  printf("en buyuk sayi: %d ", c);
  else if(a==b && a>c )   
  printf(" en buyuk sayi: %d", a);
  else if(b==c && b>c )
  printf(" en buyuk sayi: %d", b);
  else
  printf("en buyuk sayi: %d", c);

    return 0;  }  */
     
/* 
   int main () { 
   int dizi[10];
   int i;
   for(i=0;i<10;i++){
    printf("%d. sayiyi girin:",i+1);
    scanf("%d",&dizi[i]);
   }
   for(i=9;i>=0;i--){
    printf("%d\n",dizi[i]);
   }
    return 0;  } */ 

/*
   int main () {  
   float dizi[8],ort,top;
   int i;
   for(i=0;i<8;i++){
   printf("%d. sayiyi giriniz:", i+1);
   scanf("%f", &dizi[i]);
   top=top+dizi[i];
   }
   ort=top/8;
   printf("ortlamaniz eşittir: %f", ort);

   return 0;  } */

/*
  int main () {  
  int fibonacci [20];
  fibonacci[0]=1;
  fibonacci[1]=1;
  printf("fibonacci[0] = %d \n", fibonacci[0]=1);
  printf("fibonacci[1] = %d \n", fibonacci[1]=1);
  for(int i=2; i<20; i++){
    fibonacci[i]=fibonacci[i-1]+fibonacci[i-2];
    printf("fibonacci[%d] = %d \n",i,fibonacci[i]);
  }
   return 0;  } */

/*  
  int main () {
  int toplam=0,num;
  while(num!=-1) {
  printf("bir sayi giriniz:");
  scanf("%d", &num);
  if(num==-1)
           break;
   toplam += num ;     
  printf("toplam: %d \n",toplam);
  }
  return 0;  } */
  
/*  
  int main () {
  int i,j;
  for( i=1; i<=10;i++){
       for(j=1;j<=10;j++){
          printf("%d\t",i*j);
       }
       printf("\n");
  }
  return 0;  }  */ 


/* concatenate two string 

   int main() {
   char str1[]="Ridvan ";
   printf("%s", str1);
   char str2[]="Arslan";
   printf("%s", str2);
   char str3[20];
   strcat(str1,str2);
   printf(" iki string birlesimi \n %s",str3);

    return 0;  }  */


/*  
    int main() {
    char str[100];
    printf("Bir string girin: ");
    gets(str);

    int kelimeSayisi = 0;
    int aSayisi = 0;

    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] == ' ' || str[i] == '\n') {
            kelimeSayisi++;
        } else {
            if (str[i] == 'a' || str[i] == 'A') {
                aSayisi++;
            }
        }
    }
    printf("Girilen stringdeki toplam %d kelimenin %d tanesinde 'a' karakteri bulunuyor.\n", kelimeSayisi, aSayisi);

    return 0; } */ 



